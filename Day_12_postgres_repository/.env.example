# Day 12 — environment template.
# Copy to .env and fill in real values.

# Postgres DSN. Host port 5433 matches Day 8's docker-compose mapping.
DATABASE_URL=postgres://app:app@localhost:5433/appdb?sslmode=disable
